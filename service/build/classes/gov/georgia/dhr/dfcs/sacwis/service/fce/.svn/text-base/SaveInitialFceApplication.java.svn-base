package gov.georgia.dhr.dfcs.sacwis.service.fce;

import gov.georgia.dhr.dfcs.sacwis.dao.fce.FceApplicationDB;

public interface SaveInitialFceApplication {
  
  /** This service saves the initial record in FCE_APPLICATION table with the 
   * required data filled in
   * @param fceApplicationDB
   * @return int
   */
  public int saveInitialFceApplication(FceApplicationDB fceApplicationDB);
  
}
