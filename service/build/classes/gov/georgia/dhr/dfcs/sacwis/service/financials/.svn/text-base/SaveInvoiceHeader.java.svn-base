package gov.georgia.dhr.dfcs.sacwis.service.financials;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CFIN03SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CFIN03SO;

public interface SaveInvoiceHeader {
  /**
   * Saves the information for the entry fields fro CFIN03W - INVOICE HEADER. The sercive will save/update the INVOICE
   * table with the information that appears on the window.
   *
   * @param cfin03si The input object populated with the data to be persisted.
   * @return Returns a {@link CFIN03SO} with idInvoice and dtLastUpdate populated.
   */
  public CFIN03SO saveInvoiceHeaderInformation(CFIN03SI cfin03si);
}
