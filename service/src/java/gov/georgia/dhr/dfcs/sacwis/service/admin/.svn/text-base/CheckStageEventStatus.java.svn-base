package gov.georgia.dhr.dfcs.sacwis.service.admin;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CCMN06UI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CCMN06UO;

public interface CheckStageEventStatus {

  /**
   * Description: This is a Common Function (not a service) which is called by services which update functional tables.
   * It is packaged in the libappd.a archive library. This function receives the following inputs: cReqFuncCd,
   * ulIdStage, and szCdTask.
   *
   * @param ccmn06ui
   */

  public CCMN06UO status(CCMN06UI ccmn06ui);
}
