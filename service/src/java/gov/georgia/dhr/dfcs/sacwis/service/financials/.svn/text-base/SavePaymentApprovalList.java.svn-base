package gov.georgia.dhr.dfcs.sacwis.service.financials;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CFIN20SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CFIN20SO;

public interface SavePaymentApprovalList {

  /**
   * This service will save selected columns to the Invoice table. The DAM will AUD the CD INVO APPROVED, ID PERSON and
   * SYS TS LAST UPDATE columns to the Invoice table
   *
   * @param cfin20si
   * @return
   */
  CFIN20SO savePaymentApprovalList(CFIN20SI cfin20si);

}
