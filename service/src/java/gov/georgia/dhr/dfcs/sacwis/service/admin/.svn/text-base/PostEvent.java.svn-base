package gov.georgia.dhr.dfcs.sacwis.service.admin;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CCMN01UI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CCMN01UO;

public interface PostEvent {

  /**
   * Description: This common function is called to update the event table and the Event Person link table. Rows can be
   * added, updated or deleted from the event table, while the Event Person Link table, you can only add and delete.
   *
   * @param ccmn01ui
   */
  CCMN01UO postEvent(CCMN01UI ccmn01ui);
}
