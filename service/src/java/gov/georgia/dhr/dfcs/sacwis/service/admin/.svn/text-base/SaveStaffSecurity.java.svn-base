package gov.georgia.dhr.dfcs.sacwis.service.admin;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CARC18SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CARC18SO;

public interface SaveStaffSecurity {

  /**
   * Adds, updates, and deletes staff security information and staff temporary assignments.
   *
   * @param carc18si The input object that contains the method parameters.
   */
  public CARC18SO saveStaffSecurityInformation(CARC18SI carc18si);
}
