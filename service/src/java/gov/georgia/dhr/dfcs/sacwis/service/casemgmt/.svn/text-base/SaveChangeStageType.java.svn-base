package gov.georgia.dhr.dfcs.sacwis.service.casemgmt;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CSUB64SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CSUB64SO;

public interface SaveChangeStageType {

  /**
   * This service will update the Stage Table using the post event function, and update the Todo table.
   *
   * @param csub64si
   * @return CSUB64SO
   */
  CSUB64SO saveChangeStageType(CSUB64SI csub64si);
}
