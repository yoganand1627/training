package gov.georgia.dhr.dfcs.sacwis.service.admin;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CCMN03UI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CCMN03UO;

public interface CloseOpenStage {

  /**
   * This archived library function provides the necessary edits and updates required to close a stage and open a new
   * one. It generates all the required events and to-do's related with the closure of a stage and the opening of a new
   * one.
   *
   * @param ccmn03ui
   * @return A populated {@link CCMN03UO} object.
   */
  public CCMN03UO closeOpenStage(CCMN03UI ccmn03ui);
}
