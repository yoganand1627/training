package gov.georgia.dhr.dfcs.sacwis.service.common;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CCMN50SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CCMN50SO;

public interface RetrieveTaskList {
  /**
   * This service calls two DAOs used in determining the enablement of pushbuttons on the Task List window for a
   * selected task.
   *
   * @param ccmn50si The input object containing the method parameters(column values).
   * @return CCMN50SO The output object populated with returned row/column values.
   */
  public CCMN50SO findTaskListEvents(CCMN50SI ccmn50si);
}
