package gov.georgia.dhr.dfcs.sacwis.service.admin;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CSUB40UI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CSUB40UO;

public interface TodoCommonFunction {

  /**
   * Performs the business logic neccessary to execute the TODO COMMON FUNCTION. This service will help
   * standardize/formalize how Todo's are created. It will prevent hard-coding, of Todo descriptions, due dates, and
   * other data within the functional program.
   *
   * @param csub40ui The input object containing the method parameters.
   * @return CSUB40UO The output object populated with the returned column values.
   */
  public CSUB40UO audTodo(CSUB40UI csub40ui);
}
