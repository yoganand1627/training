package gov.georgia.dhr.dfcs.sacwis.service.casemgmt;

import gov.georgia.dhr.dfcs.sacwis.structs.input.CCFC11SI;
import gov.georgia.dhr.dfcs.sacwis.structs.output.CCFC11SO;

public interface SavePALTrainingService {

  /**
   * Description:   This service will update all columns for an Id Stage/ Id PAL Services from the PAL SERVICE table. It
   * will also update all columns for an Id Event from the EVENT table. It can add or modify the EVENT row.  It will
   * also retrieve the primary child for the PAL stage in order to lik the event with the primary child whem adding the
   * Services/Training event.
   *
   * @param ccfc11si
   * @return
   */
  public CCFC11SO savePALTrainningService(CCFC11SI ccfc11si);
}
