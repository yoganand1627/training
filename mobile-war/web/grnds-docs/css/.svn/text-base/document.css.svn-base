/*  This is the stylesheet used in all forms for IMPACT.
  The document.css is referenced in all HTML pages to pick up the appropriate styles.

    This cascading style sheet is archived in PVCS in folder ...grnds-docs/css
    Be sure to 'get' to your workDomain area if testing locally (default is myDomain) */

/* default all margins for all pages left, right, top, bottom on all forms to 1 inch */
@page margin1
{
  size: 8.5in 11.0in;
  margin: .25in .75in .75in .75in;
  mso-header-margin: 0in;
  mso-footer-margin: 0in;
  mso-paper-source: 0;
}

div.margin1
{
  page: margin1;
}

/* default for all styles within the body tags */
body
{ 
  font-family: Arial;
  font-size: 10pt;
  text-align: left;
  vertical-align: top;
}

/* default for all styles within the table tags */
table
{ 
  font-family: Arial;
  font-size: 10pt;
  text-align: left;
  vertical-align: top;
}

/* default for all styles within the td tags of a table */
td           
{ 
  font-family: Arial;
  font-size: 10pt;
  text-align: left;
  vertical-align: top;
  font-weight: normal;
  padding: 0in;
}

/* default for all styles within the th tags of a table */
th           
{ 
  font-family: Arial;
  font-size: 10pt;
  text-align: left;
  vertical-align: top;
  font-weight: bold;
  padding: 0in;
}

/* default for all styles within the thead tags known as table header */
thead        
{ 
  display: table-header-group;
}

/* default all margins of the p tag to prevent double spacing in templates */
p       
{ 
  margin: 0in;
}

/* default for all styles within the h1 tags known as header 1 */
h1           
{ 
  font-family: Arial;
  font-size: 12pt;
  text-align: center;
  font-weight: bold;
}

/* default for all styles within the h2 tags known as header 2 */
h2
{ 
  font-family: Arial;
  font-size: 14pt;
  text-align: center;
  font-weight: bold;
}

/* pagebreak class to force a page break in a printed HTML page */
.pagebreak
{
  page-break-after: always;
}

/* styles used for spans to stretch 100% across the screen at 10 font for user edit text */
.editComment1 
{ 
  font-family: Arial;
  font-size: 10pt;
  word-wrap: break-word;
  overflow-x: hidden;
  width: 100%;
}

/* styles used for spans to stretch 100% across the screen at 8 font for user edit text */
.editComment2
{ 
  font-family: Arial;
  font-size: 8pt;
  word-wrap: break-word;
  overflow-x: hidden;
  width: 100%;
}

/* styles used for spans for user edit text at 10 font to be inserted in line between HTML text where the width can vary */
.editTextBox1 
{ 
  font-family: Arial;
  font-size: 10pt;
  word-wrap: break-word;
  overflow: hidden;
}

/* styles used for spans for user edit text at 8 font to be inserted in line between HTML text where the width can vary */
.editTextBox2
{ 
  font-family: Arial;
  font-size: 8pt;
  word-wrap: break-word;
  overflow: hidden;
}

/* styles used for spans in a table for user edit text with a grey background for indication */
.editTabularText1 
{ 
  font-family: Arial;
  font-size: 10pt;
  word-wrap: break-word;
  overflow: hidden;
  width: 100%;
}

/* styles used for spans in a table for user edit text at 8 font with a grey background for indication */
.editTabularText2 
{ 
  font-family: Arial;
  font-size: 8pt;
  word-wrap: break-word;
  overflow: hidden;
  width: 100%;
}

/* style for Times New Roman, 12pt used in legal documents */
.tnr12pt
{ 
  font-family: Times New Roman;
  font-size: 12pt;
}
/* style for Times New Roman, 9pt used in legal documents */
.tnr9pt
{ 
  font-family: Times New Roman;
  font-size: 9pt;
}

/* fixed-width font with other parameters set like default style */
.fixedwidth
{ 
  font-family: Courier New;
  font-size: 10pt;
  text-align: left;
  vertical-align: top;
}

/* style to align the text right */
.style1 
{ 
  text-align: right;
}

/* style to align the text center */
.style2 
{ 
  text-align: center;
}

/* style to justify the text */
.style3 
{ 
  text-align: justify;
}

/* style to align the text to the bottom */
.style4 
{ 
  vertical-align: bottom;
}

/* style to put an underline beneath the text */
.style5 
{ 
  text-decoration: underline;
}

/* style to make the text italicized */
.style6 
{ 
  font-style: italic;
}

/* style to align the text center and put an underline beneath it */
.style7 
{ 
  text-align: center;
  text-decoration: underline;
}

/* style to make the font size 8 point */
.style8 
{ 
  font-size: 8pt;
}

/* style to make the font size 8 point and align the text right */
.style9 
{ 
  font-size: 8pt;
  text-align: right;
}

/* style to make the font size 8 point and align the text center */
.style10 
{ 
  font-size: 8pt;
  text-align: center;
}

/* style to make the font size 8 point and italicize the text */
.style11 
{ 
  font-size: 8pt;
  font-style: italic;
}

/* style to make the font size 8 point and align the text right and to the bottom */
.style12 
{ 
  font-size: 8pt;
  text-align: right;
  vertical-align: bottom;
}

/* style to make the font size 8 point and force the text to break (even in the middle of a word) */
.style13 
{ 
  font-size: 8pt;
  word-break: break-all;
}

/* style to make the font size 8 point, align the text center, and force the text to break (even in the middle of a word) */
.style14 
{ 
  font-size: 8pt;
  text-align: center;
  word-break: break-all;
}

/* style to make the font size 8 point, align the text center, and put an underline beneath it */
.style15
{ 
  font-size: 8pt;
  text-align: center;
  border-bottom-color: Black;
  border-bottom-style: solid;
  border-bottom-width: thin;
}

/* style to put an underline beneath the text */
.style16 
{ 
  border-bottom-color: Black;
  border-bottom-style: solid;
  border-bottom-width: thin;
}

/* style to make the font size 11 point and align the text vertically to the middle */
.style17 
{ 
  font-size: 11pt;
  vertical-align: middle;
}

/* style to make the text italicized and aligned center */
.style18 
{ 
  font-style: italic;
  text-align: center;
}

/* style to make the font size 12 point */
.style19
{
  font-size: 12pt;
}

/* style to make the font size 14 point and aligned center */
.style20
{
  font-size: 14pt;
  text-align: center;
}

/* style to make table border on all sides except the top */
.style21
{
  border-bottom-color: Grey;
  border-bottom-style: ridge;
  border-bottom-width: thin;
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
  border-right-color: Grey;
  border-right-style: ridge;
  border-right-width: thin;
}

/* style to make table border on left */
.style22
{
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
}

/* style to make table border on all sides except the top and maintain an 8 point font */
.style23
{
  font-size: 8pt;
  border-bottom-color: Grey;
  border-bottom-style: ridge;
  border-bottom-width: thin;
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
  border-right-color: Grey;
  border-right-style: ridge;
  border-right-width: thin;
}

/* style to make table border on left and maintain an 8 point font */
.style24
{
  font-size: 8pt;
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
}

/* style to make the font size 11 point, align the text vertically to the middle, make table border on all sides */
.style25 
{ 
  font-size: 11pt;
  vertical-align: middle;
  border-bottom-color: Grey;
  border-bottom-style: ridge;
  border-bottom-width: thin;
  border-top-color: Grey;
  border-top-style: ridge;
  border-top-width: thin;
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
  border-right-color: Grey;
  border-right-style: ridge;
  border-right-width: thin;
}
/* style similar to heading 2 (h2) but for section headings not part of form heading */
.style26
{ 
  font-family: Arial;
  font-size: 14pt;
  text-align: center;
  font-weight: bold;
}
/* style similar to heading 1 (h1) but for section headings not part of form heading */
.style27
{ 
  font-family: Arial;
  font-size: 12pt;
  text-align: center;
  font-weight: bold;
}
/* style to make table border on all sides except the bottom */
.style28
{
  border-left-color: Grey;
  border-left-style: ridge;
  border-left-width: thin;
  border-right-color: Grey;
  border-right-style: ridge;
  border-right-width: thin;
  border-top-color: Grey;
  border-top-style: ridge;
  border-top-width: thin;
}

#txtMaltreatment, #txtSeriousHarm, #txtChildVulnerability,
#txtCaregiverProtective, #txtSafetyIntervention, #txtEmergingDanger,
#txtRiskInterventions, #txtCGWBPermanency, #txtCHWBPermanency,
#txtContactVisit
{
	height: 70px; 
	width: 90%;
}
#txtCGSafetyChecklist, #txtCHSafetyChecklist, #txtCGWellBeing,
#txtCHWellBeing,
#txtCasePlan, #txtChildInvolvementCasePlan, #txtConcernsOrRedFlags,
#txtFollowUpOrNextSteps
{
	height: 70px; 
	width: 80%;
}

#txtPostComment
{
	height: 140px; 
	width: 90%;
}

#spanMaltreatment, #spanSeriousHarm, #spanChildVulnerability,
#spanCaregiverProtective, #spanSafetyIntervention, #spanEmergingDanger,
#spanRiskInterventions, #spanCGWBPermanency, #spanCHWBPermanency,
#spanCasePlan, #spanChildInvolvementCasePlan, #spanFollowUpOrNextSteps,
#spanPostComment, #spanBlankNarrative, #spanPostComment, #spanContactVisit,
#spanCGSafetyChecklist, #spanCHSafetyChecklist, #spanCGWellBeing,
#spanCHWellBeing, #spanConcernsOrRedFlags
{
	display: none;
}

#txtBlankNarrative, #txtPostComment
{
	font-family: Arial;
	font-size: 10pt;
	word-wrap: break-word;
	overflow-x: hidden;
	height: 100px; 
	width: 90%;
}

