//This javascript file was generated from the constraints.xsd file.
//To modify, make changes in the constraints file or generation routine and then  regenerate.

var reName=/^[A-Z][A-Za-z\s\-\.']+$/
function isValidName(str)
{ return ( str.length == 0 || (reName.test(str)  &&  str.length  <= 50 &&  str.length  >= 1 ) );
}

var reEmail=/^[\w\-\.]+@[\w\-\.]+$/
function isValidEmail(str)
{ return ( str.length == 0 || (reEmail.test(str)  &&  str.length  <= 70 &&  str.length  >= 7 ) );
}

var rePhoneExtension=/^\w+$/
function isValidPhoneExtension(str)
{ return ( str.length == 0 || (rePhoneExtension.test(str)  &&  str.length  <= 8 &&  str.length  >= 1 ) );
}

var reAddress=/^[\w\s\.]+$/
function isValidAddress(str)
{ return ( str.length == 0 || (reAddress.test(str)  &&  str.length  <= 30 &&  str.length  >= 1 ) );
}

var reCity=/^[A-Z,a-z,\s]+$/
function isValidCity(str)
{ return ( str.length == 0 || (reCity.test(str)  &&  str.length  <= 20 &&  str.length  >= 1 ) );
}

var reApartment=/^\w+$/
function isValidApartment(str)
{ return ( str.length == 0 || (reApartment.test(str)  &&  str.length  <= 10 &&  str.length  >= 1 ) );
}

var reZip=/^\d+$/
function isValidZip(str)
{ return ( str.length == 0 || (reZip.test(str)  &&  str.length  <= 10 &&  str.length  >= 5 ) );
}

var rePOBox=/^\w+$/
function isValidPOBox(str)
{ return ( str.length == 0 || (rePOBox.test(str)  &&  str.length  <= 20 &&  str.length  >= 5 ) );
}

var reDate=/^\d{1,2}\/\d{1,2}\/\d{4}$/
function isValidDate(str)
{ return ( str.length == 0 || (reDate.test(str)  &&  str.length  <= 10 &&  str.length  >= 8 ) );
}

var reDay=/^\d{2}$/
function isValidDay(str)
{ return ( str.length == 0 || (reDay.test(str)  &&  str.length  <= 2 &&  str.length  >= 2 ) );
}

var reYear=/^\d{4}$/
function isValidYear(str)
{ return ( str.length == 0 || (reYear.test(str)  &&  str.length  <= 4 &&  str.length  >= 4 ) );
}

var reMonthNumber=/^\d{2}$/
function isValidMonthNumber(str)
{ return ( str.length == 0 || (reMonthNumber.test(str)  &&  str.length  <= 2 &&  str.length  >= 2 ) );
}

var reTime=/^\d{2}:\d{2}$/
function isValidTime(str)
{ return ( str.length == 0 || (reTime.test(str)  &&  str.length  <= 5 &&  str.length  >= 5 ) );
}

var reLetter=/^[A-Za-z]$/
function isValidLetter(str)
{ return ( str.length == 0 || (reLetter.test(str)  &&  str.length  <= 1 &&  str.length  >= 1 ) );
}

var reDigit5=/^\d{5}$/
function isValidDigit5(str)
{ return ( str.length == 0 || (reDigit5.test(str)  &&  str.length  <= 5 &&  str.length  >= 5 ) );
}

var reDigit4=/^\d{4}$/
function isValidDigit4(str)
{ return ( str.length == 0 || (reDigit4.test(str)  &&  str.length  <= 4 &&  str.length  >= 4 ) );
}

var reDigit3=/^\d{3}$/
function isValidDigit3(str)
{ return ( str.length == 0 || (reDigit3.test(str)  &&  str.length  <= 3 &&  str.length  >= 3 ) );
}

var reDigit2=/^\d{2}$/
function isValidDigit2(str)
{ return ( str.length == 0 || (reDigit2.test(str)  &&  str.length  <= 2 &&  str.length  >= 2 ) );
}

var reDigit1=/^\d$/
function isValidDigit1(str)
{ return ( str.length == 0 || (reDigit1.test(str)  &&  str.length  <= 1 &&  str.length  >= 1 ) );
}

var reDouble=/^\d+\.\d+$/
function isValidDouble(str)
{ return ( str.length == 0 || (reDouble.test(str)  ) );
}

var reMoney=/^\d+\.\d{2}$/
function isValidMoney(str)
{ return ( str.length == 0 || (reMoney.test(str)  ) );
}

var reAlpha=/^[A-Za-z]+$/
function isValidAlpha(str)
{ return ( str.length == 0 || (reAlpha.test(str)  ) );
}

var reNumeric=/^\d+$/
function isValidNumeric(str)
{ return ( str.length == 0 || (reNumeric.test(str)  ) );
}

var reAlphaNumeric=/^[A-Za-z0-9]+$/
function isValidAlphaNumeric(str)
{ return ( str.length == 0 || (reAlphaNumeric.test(str)  ) );
}

var reParagraph=/^[\w\s\.\?\!:;'",\-\(\)]+$/
function isValidParagraph(str)
{ return ( str.length == 0 || (reParagraph.test(str)  ) );
}

var reLowerCase=/^[a-z]+$/
function isValidLowerCase(str)
{ return ( str.length == 0 || (reLowerCase.test(str)  ) );
}

var reUpperCase=/^[A-Z]+$/
function isValidUpperCase(str)
{ return ( str.length == 0 || (reUpperCase.test(str)  ) );
}

function isValidUsername(str)
{ return ( str.length == 0 || ( str.length  <= 70 &&  str.length  >= 5 ) );
}

function isValidPassword(str)
{ return ( str.length == 0 || ( str.length  >= 5 ) );
}

var reNameMultipleWords=/^[a-zA-Z0-9\s\-\.' ]+$/
function isValidNameMultipleWords(str)
{ return ( str.length == 0 || (reNameMultipleWords.test(str)  &&  str.length  >= 2 ) );
}

function isValidAny15(str)
{ return ( str.length == 0 || ( str.length  <= 15 &&  str.length  >= 0 ) );
}

var reDigit9=/^\d{9}$/
function isValidDigit9(str)
{ return ( str.length == 0 || (reDigit9.test(str)  &&  str.length  <= 9 &&  str.length  >= 9 ) );
}

var reAlphaNumeric14=/^[A-Za-z0-9]+$/
function isValidAlphaNumeric14(str)
{ return ( str.length == 0 || (reAlphaNumeric14.test(str)  &&  str.length  <= 14 &&  str.length  >= 14 ) );
}

var reDriversLicense=/^[A-Z]{2}\d{4,15}$/
function isValidDriversLicense(str)
{ return ( str.length == 0 || (reDriversLicense.test(str)  &&  str.length  <= 15 &&  str.length  >= 5 ) );
}

var reMedicareNumber=/^\d{9}[A-Za-z]{2}$/
function isValidMedicareNumber(str)
{ return ( str.length == 0 || (reMedicareNumber.test(str)  &&  str.length  <= 11 &&  str.length  >= 11 ) );
}

var rePaceProjectClientNumber=/^P\wCE$/
function isValidPaceProjectClientNumber(str)
{ return ( str.length == 0 || (rePaceProjectClientNumber.test(str)  &&  str.length  <= 4 &&  str.length  >= 4 ) );
}

var reSocialSecurityNumber=/^\d+$/
function isValidSocialSecurityNumber(str)
{ return ( str.length == 0 || (reSocialSecurityNumber.test(str)  &&  str.length  <= 9 &&  str.length  >= 9 ) );
}

var reParagraph40=/^[\w\s\.\?\!:;'",\-\(\)]+$/
function isValidParagraph40(str)
{ return ( str.length == 0 || (reParagraph40.test(str)  &&  str.length  <= 40 &&  str.length  >= 0 ) );
}

var reName12=/^[A-Za-z\s\-\.']+$/
function isValidName12(str)
{ return ( str.length == 0 || (reName12.test(str)  &&  str.length  <= 12 &&  str.length  >= 1 ) );
}

var reName22=/^[A-Za-z\s\-\.']+$/
function isValidName22(str)
{ return ( str.length == 0 || (reName22.test(str)  &&  str.length  <= 22 &&  str.length  >= 1 ) );
}

var reName20=/^[A-Za-z\s\-\.' ]+$/
function isValidName20(str)
{ return ( str.length == 0 || (reName20.test(str)  &&  str.length  <= 20 &&  str.length  >= 0 ) );
}

var reName25=/^[A-Za-z\s\-\.' ]+$/
function isValidName25(str)
{ return ( str.length == 0 || (reName25.test(str)  &&  str.length  <= 25 &&  str.length  >= 0 ) );
}

var reDigit10=/^\d{10}$/
function isValidDigit10(str)
{ return ( str.length == 0 || (reDigit10.test(str)  &&  str.length  <= 10 &&  str.length  >= 10 ) );
}

var reComments=/^[\w\s\.\?\!:;'",\-\(\)]+$/
function isValidComments(str)
{ return ( str.length == 0 || (reComments.test(str)  &&  str.length  <= 300 &&  str.length  >= 0 ) );
}

var reAlphaNumeric2=/^[A-Za-z0-9]+$/
function isValidAlphaNumeric2(str)
{ return ( str.length == 0 || (reAlphaNumeric2.test(str)  &&  str.length  <= 2 &&  str.length  >= 2 ) );
}

var reParagraph80=/^[\w\s\.\?\!:;'",\-\(\)]+$/
function isValidParagraph80(str)
{ return ( str.length == 0 || (reParagraph80.test(str)  &&  str.length  <= 80 &&  str.length  >= 0 ) );
}

var reParagraph30=/^[\w\s\.\?\!:;'",\-\(\)]+$/
function isValidParagraph30(str)
{ return ( str.length == 0 || (reParagraph30.test(str)  &&  str.length  <= 30 ) );
}

var reIncome=/^\d,+$/
function isValidIncome(str)
{ return ( str.length == 0 || (reIncome.test(str)  &&  str.length  <= 13 ) );
}

var reAlphaNumeric4=/^[A-Za-z0-9]+$/
function isValidAlphaNumeric4(str)
{ return ( str.length == 0 || (reAlphaNumeric4.test(str)  &&  str.length  <= 4 ) );
}

var reAlphaNumeric8=/^[A-Za-z0-9]+$/
function isValidAlphaNumeric8(str)
{ return ( str.length == 0 || (reAlphaNumeric8.test(str)  &&  str.length  <= 8 ) );
}

var reDigit16Less=/^\d+$/
function isValidDigit16Less(str)
{ return ( str.length == 0 || (reDigit16Less.test(str)  &&  str.length  <= 16 ) );
}

var reDigit2Less=/^\d+$/
function isValidDigit2Less(str)
{ return ( str.length == 0 || (reDigit2Less.test(str)  &&  str.length  <= 2 ) );
}

var reDigit3Less=/^\d+$/
function isValidDigit3Less(str)
{ return ( str.length == 0 || (reDigit3Less.test(str)  &&  str.length  <= 3 ) );
}

var reName50=/^[A-Za-z\s\-\.']+$/
function isValidName50(str)
{ return ( str.length == 0 || (reName50.test(str)  &&  str.length  <= 50 ) );
}

var reName30=/^[A-Za-z\s\-\.']+$/
function isValidName30(str)
{ return ( str.length == 0 || (reName30.test(str)  &&  str.length  <= 30 ) );
}

var reAgeYear=/^\d+$/
function isValidAgeYear(str)
{ return ( str.length == 0 || (reAgeYear.test(str)  ) );
}

var reAgeMonth=/^\d+$/
function isValidAgeMonth(str)
{ return ( str.length == 0 || (reAgeMonth.test(str)  ) );
}
